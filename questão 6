/*Declare uma struct para representar as características/propriedades de um atleta de seu esporte favorito.
Crie uma função que exiba as propriedades de maneira bem organizada e formatada.
Crie três exemplos de atletas variando as propriedades e teste a função de exibição.*/
#include <stdio.h>
#include <string.h>
struct sporte {
    char nome[50];
    int salto;
    int acrobacia;
    int pirueta;
};
void Atleta(struct sporte a) { //a é o parametro que a função recebe, esse parâmetro é do tipo struct Sporte
    printf("Dados do atleta:\nNome: %s \n", a.nome);
    printf("Salto     : %d\n", a.salto);
    printf("Acrobacia : %d\n", a.acrobacia);
    printf("Pirueta   : %d\n", a.pirueta);
}
int main(void){
    struct sporte atl[3];
    int i=0;
    //int j=0;
    for (i=0;i<3;i++){
        printf("Digite o nome do atleta: ");
        fgets(atl[i].nome, sizeof(atl[i].nome), stdin);
        //while (j)
        printf("Digite a nota do SALTO: ");
        scanf("%d", &atl[i].salto);

        printf("Digite a nota da ACROBACIA: ");
        scanf("%d", &atl[i].acrobacia);

        printf("Digite a nota da PIRUETA: ");
        scanf("%d", &atl[i].pirueta);
        
        Atleta(atl[i]);
        getchar();//evitar o erro de ignorar o nome no fgets (precisa botar)

    }
    //j++;
    for (i = 0; i < 3; i++) {
        Atleta(atl[i]);//não é &a pq ele não é um ponteiro, ele vai mandar todas as informaçoes de a1 na ordem que aparecem para a funçao atleta
    }
}
    
/*struct Pessoa {
    char nome[30];
    int idade;
};
int main() {
    struct Pessoa p;
    fgets(p.nome, sizeof(p.nome), stdin);  // acessando campo 'nome'
    p.idade = 25;                          // acessando campo 'idade'
}*/
